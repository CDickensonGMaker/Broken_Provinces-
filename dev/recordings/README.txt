Movie Maker Mode writes here (project.godot movie_writer/movie_file).
The .gitignore already ignores every video format; this file keeps the
directory present so Godot has somewhere to open its output file.
